// EmberDisplay — Future $EMBER balance and contribution display
// Phase 3+: Read from on-chain SPL token balance via Privy wallet SDK
// For now: reads from static data/hearth_state.json
// EMBER is utility / service weight / contribution signal. Never investment language.

export default function EmberDisplay() {
  return null
}
